# 协议编译器依赖
# 核心编译器不依赖任何第三方库（纯 Python 标准库实现）

# 可选依赖（用于 LLM 桥接、Web 服务等高级功能）
requests>=2.28.0  # LLM API 调用（Kimi / DeepSeek）
flask>=2.3.0     # Web 界面 / HTTP 服务
bs4>=0.0.1        # HTML 解析（搜索结果处理）
html2text>=2024.2.26  # HTML 转 Markdown
